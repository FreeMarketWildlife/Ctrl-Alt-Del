const fs=require('fs'),path=require('path');const {createCanvas,loadImage}=require('@napi-rs/canvas');const ROOT=path.resolve(__dirname,'..');
(async()=>{const c=createCanvas(1192,996),x=c.getContext('2d');x.fillStyle='#101a18';x.fillRect(0,0,c.width,c.height);x.fillStyle='#d4ccad';x.font='22px sans-serif';x.fillText('RECYKE / TWO INDEPENDENT NATIVE FAMILIES',24,33);x.font='12px monospace';x.fillStyle='#a2b1a7';x.fillText('Same source-pixel size in every panel. No family is resized. Figures retain their approved source artwork.',24,56);
 for(const [family,left,w] of [['mini',24,480],['big',528,640]]){x.fillStyle='#b7cc8b';x.font='15px sans-serif';x.fillText(family==='mini'?'MINI / 32px · current Proposal A':'BIG / 96px · historical approved appearance',left,85);for(const [file,floor] of [['street',410],['entrance-open',898]]){const im=await loadImage(path.join(ROOT,'review',`${family}-${file}.png`)),y=floor-(family==='mini'?222:308);x.drawImage(im,left,y);x.strokeStyle='#3c5147';x.strokeRect(left-.5,y-.5,im.width+1,im.height+1);}}
 x.font='13px sans-serif';x.fillStyle='#d4ccad';x.fillText('01 / Main street',24,471);x.fillText('02 / Concealed Moral Code entrance — open visual state',24,975);fs.writeFileSync(path.join(ROOT,'review/family-comparison.png'),c.toBuffer('image/png'));})();
